import { Card } from './card';
export class User {
    constructor(nume, prenume, email, parola, account, card) {
        this.nume = nume;
        this.prenume = prenume;
        this.email = email;
        this.parola = parola;
        this.account = account;
        this.card = card;
    }
}

// clasa cu date pentu logare care descrie userul
